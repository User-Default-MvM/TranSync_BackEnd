# ==========================================
# TRANSYNC BACKEND - RAILWAY PRODUCTION ENV
# ==========================================
#
# INSTRUCCIONES PARA RAILWAY:
# 1. Copiar este archivo como .env.railway
# 2. Reemplazar los valores con los reales de producción
# 3. Railway automáticamente inyectará las variables de MySQL

# Configuración del servidor
NODE_ENV="production"
PORT="8080"

# Base de Datos MySQL (Railway Plugin - Configurar en Railway Dashboard)
DB_HOST="maglev.proxy.rlwy.net"
DB_USER="root"
DB_PASSWORD="tu_password_real_aqui"
DB_DATABASE="railway"
DB_PORT="25678"
DB_CONNECTION_LIMIT=10
DB_CONNECTION_TIMEOUT=60000
DB_ACQUIRE_TIMEOUT=60000

# Clave secreta para JWT (Generar una fuerte)
JWT_SECRET="tu_jwt_secret_aqui"

# Configuración de Email (Gmail o SendGrid)
EMAIL_USER="tu_email@gmail.com"
EMAIL_PASS="tu_password_de_aplicacion"

# SendGrid (RECOMENDADO para Railway)
SENDGRID_API_KEY="SG.tu_sendgrid_api_key_real_aqui"

# Email desde el que se envían los correos
EMAIL_FROM=noreply@tudominio.com

# Configuración de APIs de Mapas (OPCIONAL)
GOOGLE_MAPS_API_KEY=tu_google_maps_api_key_aqui
MAPBOX_API_KEY=tu_mapbox_api_key_aqui
MAP_PROVIDER=openstreetmap

# URL del frontend (Tu dominio de Netlify/Vercel)
FRONTEND_URL=https://tu-frontend.netlify.app

# URL del backend (Railway la proporciona automáticamente)
RAILWAY_PUBLIC_DOMAIN=${{RAILWAY_PUBLIC_DOMAIN}}

# ==========================================
# NOTAS IMPORTANTES:
# ==========================================
#
# 1. NUNCA subas este archivo con credenciales reales a Git
# 2. Configura estas variables en Railway Dashboard
# 3. Railway inyectará automáticamente las variables de MySQL
# 4. Usa SendGrid para mejor confiabilidad de emails
#
# ==========================================