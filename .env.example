# ==========================================
# TRANSYNC BACKEND - CONFIGURACIÓN DE ENTORNO
# ==========================================
# 
# INSTRUCCIONES:
# 1. Copiar este archivo como .env
# 2. Completar todos los valores según tu entorno local
# 3. NUNCA subir el archivo .env al repositorio

# Configuración del servidor
PORT=5000

# Configuración de la base de datos MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=tu_password_mysql_aqui
DB_DATABASE=transync
DB_PORT=3306

# Configuración avanzada del pool de conexiones (opcional)
DB_CONNECTION_LIMIT=10
DB_CONNECTION_TIMEOUT=60000
DB_ACQUIRE_TIMEOUT=60000

# NOTA: Si tu MySQL local usa puerto 3307, cambia DB_PORT=3307
# Esto es común en algunas instalaciones de XAMPP/WAMP

# Clave secreta para JWT (generar una única por proyecto)
JWT_SECRET=genera_una_clave_secreta_fuerte_aqui

# Entorno de trabajo
NODE_ENV=development

# Configuración de Nodemailer (para envío de correos)
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_app_password_aqui

# Configuración de APIs de Mapas
# Google Maps API (recomendado)
GOOGLE_MAPS_API_KEY=tu_google_maps_api_key_aqui

# Mapbox API (alternativa)
MAPBOX_API_KEY=tu_mapbox_api_key_aqui

# Proveedor de mapas por defecto (google, mapbox o openstreetmap)
MAP_PROVIDER=openstreetmap

# URL del frontend (para CORS)
FRONTEND_URL=http://localhost:3000

# ==========================================
# NOTAS PARA EL EQUIPO:
# ==========================================
#
# MYSQL:
# - Asegurar que MySQL esté corriendo
# - Crear la base de datos 'transync'
# - Ejecutar el script SQL para crear las tablas
#
# GMAIL (opcional, para recuperación de contraseñas):
# - Habilitar autenticación de 2 pasos en Gmail
# - Generar una contraseña de aplicación
# - Usar esa contraseña en EMAIL_PASS
#
# PUERTOS COMUNES:
# - MySQL por defecto: 3306
# - XAMPP MySQL: 3307
# - WAMP MySQL: 3306
# - MAMP MySQL: 3306 o 8889