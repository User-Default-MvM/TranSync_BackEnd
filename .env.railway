# ==========================================
# TRANSYNC BACKEND - RAILWAY PRODUCTION ENV
# ==========================================
#
# INSTRUCCIONES PARA RAILWAY:
# 1. Copiar estas variables al dashboard de Railway
# 2. Reemplazar los valores con los reales de producción
# 3. Railway automáticamente inyectará las variables de MySQL

# Configuración del servidor
NODE_ENV=production

# Base de Datos MySQL (Railway Plugin - Auto-generadas)
DB_HOST=${{MySQL.MYSQL_HOST}}
DB_USER=${{MySQL.MYSQL_USER}}
DB_PASSWORD=${{MySQL.MYSQL_PASSWORD}}
DB_DATABASE=${{MySQL.MYSQL_DATABASE}}
DB_PORT=${{MySQL.MYSQL_PORT}}

# Configuración avanzada del pool de conexiones
DB_CONNECTION_LIMIT=10
DB_CONNECTION_TIMEOUT=60000
DB_ACQUIRE_TIMEOUT=60000

# Clave secreta para JWT (OBLIGATORIO - Generar una fuerte)
JWT_SECRET=AfdLpgErEf1%

# Configuración de Nodemailer (OPCIONAL - Fallback)
EMAIL_USER=geminipruebas7@gmail.com
EMAIL_PASS=gcsb hxpe jqsr vhsg

# SendGrid (RECOMENDADO para Railway - más confiable)
# REEMPLAZA con tu API key real de SendGrid
SENDGRID_API_KEY=SG.your_actual_sendgrid_api_key_here

# Configuración de APIs de Mapas (OPCIONAL)
GOOGLE_MAPS_API_KEY=tu_google_maps_api_key_aqui
MAPBOX_API_KEY=tu_mapbox_api_key_aqui
MAP_PROVIDER=openstreetmap

# URL del frontend (ACTUALIZAR con tu dominio de Railway)
FRONTEND_URL=https://tu-frontend-domain.railway.app

# URL del backend (Railway la proporciona automáticamente)
RAILWAY_PUBLIC_DOMAIN=${{RAILWAY_PUBLIC_DOMAIN}}

# ==========================================
# NOTAS IMPORTANTES PARA RAILWAY:
# ==========================================
#
# 1. MYSQL PLUGIN:
#    - Agregar MySQL database desde Railway dashboard
#    - Las variables DB_* se generan automáticamente
#    - No modificar las variables que empiezan con ${{MySQL.*}}
#
# 2. JWT_SECRET:
#    - OBLIGATORIO cambiar por una clave fuerte
#    - Mínimo 32 caracteres
#    - Usar generador de claves seguras
#
# 3. FRONTEND_URL:
#    - Actualizar con la URL real de tu frontend
#    - Importante para configuración CORS
#
# 4. EMAIL (Opcional):
#    - Solo necesario si usas recuperación de contraseñas
#    - Usar contraseña de aplicación de Gmail
#    - O usar SendGrid para mejor confiabilidad
#
# 5. MAPAS (Opcional):
#    - Solo si necesitas funcionalidad de mapas avanzada
#    - OpenStreetMap funciona sin API key
#
# ==========================================
# VARIABLES CRÍTICAS QUE DEBES CAMBIAR:
# ==========================================
# - JWT_SECRET (OBLIGATORIO)
# - FRONTEND_URL (Actualizar con tu dominio)
# - SENDGRID_API_KEY (Si usas SendGrid)
# - API keys de mapas (Si los necesitas)